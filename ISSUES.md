# Issue Tracking: Upstream vs. cosmic-vulkan

Bugs found while developing and stress-testing this project's Vulkan renderer,
tracked against their actual root cause and upstream status. This exists
because several of these bugs turned out to be pre-existing in upstream
`cosmic-comp`/Smithay rather than caused by the Vulkan renderer work itself -
this table is the record of which is which, so a bug doesn't get
misattributed to "Vulkan is broken" when it reproduces identically on GLES,
and so a real regression doesn't get dismissed as "that's just upstream."

Each row links to this project's own tracking issue (full investigation,
evidence, and fix status) and any known upstream issue/PR for the same bug.

Legend - **Introduced by**: `upstream` (pre-existing in `pop-os/cosmic-comp`,
Smithay, or their packaging; reproduces without this project's changes) ·
`this project` (caused by a commit in this project's branches) ·
`environment` (machine/build setup, not compositor code) · `n/a` (not a
regression, a missing feature) · ⚪ not yet determined. **Status**: 🔴
open/unfixed · 🟡 fix applied, unverified · 🟢 fixed and verified · ⚪ not yet
investigated.

| Issue | Introduced by | Status | Upstream Issues | Upstream PRs | This project's tracking |
|---|---|---|---|---|---|
| **XWayland `CreateNotify`/`MapRequest` race aborts the XWM event handler** ([research/issue-4-xwayland-createnotify-race.md](research/issue-4-xwayland-createnotify-race.md)). Mechanism: Smithay handles `CreateNotify` with synchronous checked X11 round-trips on the compositor main thread (= input thread); a short-lived helper window (Steam tooltips/popups, Wine/Proton helper windows, every fullscreen/resolution toggle) destroyed before the reply returns `BadWindow`/`BadDrawable`, the handler returns `Err`, the whole event is dropped (`warn!("Failed to handle X11 event")`), and cosmic-comp's focus fix-up can land on `Restoring focus to None` so input has nowhere to route. Introduced by Smithay #2037 (2026-05-22); the only examined XWM (vs wlroots/weston/KWin/mutter/i3) that round-trips at `CreateNotify`. Reliable repro: toggle fullscreen/resolution in a Proton game (~107-line burst, 2026-09-17); also the bare Steam client at startup/self-restart.<br>Sub-items: (a) handler abort - **fixed** in the fork (`88eb9c49`, `5af7527e`: `reply_or_window_gone` tolerates `Window \| Drawable` errors in `CreateNotify`/`MapRequest`); (b) redundant `CreateNotify` `get_geometry` round-trip - **removed** (`88eb9c49`, the event carries geometry); (c) ~15 synchronous round-trips remain (`get_window_attributes` + `update_properties`' ~13 `get_property`) - **open**, the async refactor (research doc bucket G / §20 staged design: zero I/O at `CreateNotify`, one pipelined batch at `MapRequest`); (d) §20.4 re-pairing hypothesis (window gets a new `wl_surface`, no leave/enter) - **untested**, needs an explicit leave/enter at `set_wl_surface`, not an equality change; (e) `GetAtomName` → `BadAtom` bursts - seen, unexplained, minor; (f) unsolicited `Got X11 Error` for requests queued against dead windows - expected, universal across XWMs, informational only.<br>Regression caused by this project (2026-09-19): `635a8884` compared `wl_surface` identity in `X11Surface::eq` while holding two locks on one shared `Arc<Mutex>` - std `Mutex` is not reentrant, so the main thread self-deadlocked on the first pointer motion over an X11 client (Steam launch froze the screen; zero context switches, `futex_do_wait`, no log line). Hot-fixed in `e6f035a6`, then audit (§24) showed the whole change was a no-op (one `X11Surface` per window, clones share state) and it was reverted (`842687df`). Lesson: any `X11Surface` locking/equality change needs a live XWayland client test; `cargo check` cannot catch it.<br>Verified 2026-09-19 22:37-22:44 on `5af7527e`: Steam launch + self-restart + Mass Effect: Andromeda launch/alt-tab/close → `Failed to handle X11 event` 0 (was constant), `Restoring focus to None` 0 (was 2, coinciding with input loss), no stall, no coredumps; 222 unsolicited `Got X11 Error` and 26 `no matching wl_surface` remain as expected.<br>History: the user-visible family ("mouse/input breaks in XWayland games on COSMIC") goes back to cosmic-comp#109 (2023-05) and is at least five compounding mechanisms (A geometry/stacking desync, B client-scaling vs input-region, C incomplete pointer-constraints until #2357, D Smithay XWM focus changes shipping as undocumented regressions, E the un-root-caused "only relogin fixes it" bucket); this mechanism can only explain post-2026-06 reports. | upstream | 🟢 abort fixed & verified 2026-09-19 / 🔴 (c) async refactor and (d) re-pairing open | **cosmic-comp** (symptom cluster):<br>[#1874](https://github.com/pop-os/cosmic-comp/issues/1874), [#1692](https://github.com/pop-os/cosmic-comp/issues/1692), [#1725](https://github.com/pop-os/cosmic-comp/issues/1725), [#2355](https://github.com/pop-os/cosmic-comp/issues/2355), [#2770](https://github.com/pop-os/cosmic-comp/issues/2770), [#109](https://github.com/pop-os/cosmic-comp/issues/109), [#475](https://github.com/pop-os/cosmic-comp/issues/475), [cosmic-epoch#2021](https://github.com/pop-os/cosmic-epoch/issues/2021) (+ ~20 more in the research doc)<br>**same error class elsewhere** (for comparison): [wlroots#2906](https://github.com/swaywm/wlroots/issues/2906), [wlroots#3461](https://gitlab.freedesktop.org/wlroots/wlroots/-/issues/3461), [sway#5227](https://github.com/swaywm/sway/issues/5227) | **Smithay**: [#2037](https://github.com/Smithay/smithay/pull/2037) (introduced the round-trip), [#2029](https://github.com/Smithay/smithay/pull/2029), [#1987](https://github.com/Smithay/smithay/pull/1987)<br>**cosmic-comp** (related symptom, other mechanisms): [#2357](https://github.com/pop-os/cosmic-comp/pull/2357), [#2255](https://github.com/pop-os/cosmic-comp/pull/2255) | [#4](https://github.com/Pheoxy/cosmic-vulkan/issues/4) |
| **Pointer lock not re-activated when keyboard focus returns to a Proton game** (alt-tab back, or a transient focus-steal such as Steam's `Adding process for gameID` window). Mechanism confirmed by the 2026-09-20 constraint trace (`ff04b1ee`): `update_focus_state` deactivates the lock on every keyboard-focus change (`deactivate on keyboard focus change`), nothing re-activates it when focus returns, and cosmic-comp only re-activates on the next pointer motion that is *in region and focused* - observed lock-inactive windows of 3-10 s (surfaces 159, 153, 36) before a motion happened to re-arm it. Matches the third hunk of the unmerged upstream fix. Workarounds: re-toggle fullscreen; `PROTON_ENABLE_WAYLAND=1`. Fix planned: re-activate persistent constraints in `update_focus_state` on focus return and don't deactivate for non-interactive focus stealers. | upstream | 🔴 open, mechanism confirmed live 2026-09-20 | [pop-os/cosmic-comp#1874](https://github.com/pop-os/cosmic-comp/issues/1874) (symptom family) | [pop-os/cosmic-comp#2255](https://github.com/pop-os/cosmic-comp/pull/2255) (open, unmerged) | not yet filed |
| **Mouse-look snaps between two positions ("spazzing") in Proton games** (Mass Effect: Andromeda; first seen 2026-09-19, reproduced 2026-09-20 with the pointer lock *active* and no focus change, so the lock-deactivation row above is not its only trigger). X-side capture 2026-09-20 (`xinput test-xi2 --root`, `xdotool getmouselocation` at 10 Hz) shows Xwayland's warp emulation behaving exactly per its source (`hw/xwayland/xwayland-input.c` 24.1.13: pointer parked at the window centre `5119,720`, relative deltas applied, game warps back; hints stream to cosmic-comp) and ~1,000 `Motion`+`RawMotion`/s reaching the game's toplevel - i.e. compositor → Xwayland → X client delivery is correct. Compositor-side candidates: bursts of late pointer motion (see the input-latency row) turning per-frame deltas into jitter. What Wine/Proton does with the events is uninstrumented. Next: `PROTON_LOG=1 WINEDEBUG=+cursor,+event` for a 10 s sample, and a `PROTON_ENABLE_WAYLAND=1` control run (bypasses Xwayland entirely). | ⚪ not yet determined (compositor latency vs Wine/Proton) | 🔴 open, reproduced 2026-09-20; X-side delivery proven correct | [pop-os/cosmic-comp#1692](https://github.com/pop-os/cosmic-comp/issues/1692), [pop-os/cosmic-comp#2770](https://github.com/pop-os/cosmic-comp/issues/2770) (symptom family) | n/a | not yet filed |
| **Proton game menus ignore the mouse while the lock is released** (Mass Effect: Andromeda, 2026-09-20). The game destroys its lock for a menu (`remove_constraint … Destroyed(Locked)`), cosmic-comp keeps dispatching motion, and the X side receives ~1,000 `Motion`/s with correct absolute root coordinates over the game window (10:56:35-37) - yet the menu does not react and the cursor shown is cosmic's, not the game's. Moving the pointer to the other output and back (forced leave/enter) did not help, ruling out the stale-`focus_window` theory (Xwayland drops motion only while `focus_window` is NULL). Remaining suspects: Wine/Proton's consumption of un-clipped motion, or the cursor-serial mismatch in the next row. Same next steps as the row above. | ⚪ not yet determined | 🔴 open, reproduced 2026-09-20; compositor and Xwayland delivery verified | n/a | n/a | not yet filed |
| **X11 client cursor never replaces the compositor cursor over a Proton game** (2026-09-20: cosmic's arrow stays over the game window; Steam's own cursors do show elsewhere). Smithay honours `wl_pointer.set_cursor` only when its serial equals the last `enter` serial and the focus client matches (`src/wayland/seat/pointer.rs`, `allow_setting_cursor`); any extra enter/leave the compositor sends (focus fix-ups, constraint hint warps) makes Xwayland's request carry a stale serial and it is silently dropped, and Xwayland does not re-send until the X cursor changes or the next enter. Unconfirmed until instrumented. Next build: log rejected `SetCursor` requests with serial / last-enter / client. | ⚪ not yet determined (Smithay serial check vs Xwayland) | 🔴 open, probe planned | n/a | n/a | not yet filed |
| **Pointer motion dispatched late on the compositor main thread** (user reports: Steam hover lag; mouse jitter with Firefox+Steam+Discord+Spotify open at low CPU, 2026-09-19). Measured 2026-09-20 with the `pointer motion dispatched late` probe (`ff04b1ee`): in-game baseline 850-1,700 late events per 5 s at 45-97 ms with the lock active and **no** X11 or capture activity; only ~30 slow X11 events (`CreateNotify` up to 19 ms) in the same window, so the XWM round-trips (row 1c) are a contributor, not the bulk. Contributing facts: the mouse reports at ~1 kHz, and every libinput event schedules a render on every output; during a screen share the render threads' readback contends with the shell lock (4,572 late motions in 62 s). Next probe: split `shell.write()` lock-wait from work per `PointerMotion`. Alternative/compounding explanation for the four-apps case is zram page-fault latency under `swappiness=1` (those apps were the top swap holders) - distinguishable via `/proc/pressure/memory`. | ⚪ not yet determined | 🔴 open, measured 2026-09-20; cause not yet isolated | n/a | n/a | not yet filed |
| **Fork's `pointer_constraints.rs` lacks upstream `5c93094`** - the reentrant-lock deadlock shape for oneshot locks that carry a cursor hint (found in the 2026-09-19 fork audit). Not hit by Xwayland, which only uses persistent locks; native Wayland games using oneshot locks could. Fix: cherry-pick `5c93094` when the pointer-constraints changes for the rows above land. | this project (missing upstream commit) | 🔴 open, not reproduced (no oneshot-lock client tested) | n/a | upstream cosmic-comp commit `5c93094` | not yet filed |
| **`apply_cursor_hint` emits a zero-delta `relative_motion` on a hint warp** - the pointer-constraints spec says a cursor-position hint must not generate motion. Also present upstream. Minor; matters only for clients that treat every relative event as input. | upstream | 🔴 open, minor | n/a | n/a | not yet filed |
| **X11 client cursors render at ~2x size on NixOS** (Steam hover cursors, Wine/Proton game menus; 2026-09-20). `cosmic-comp` sets `Xcursor.size`/`Xcursor.theme` on the Xwayland display by spawning `xrdb -merge` (`src/xwayland.rs`, upstream PR #1976) and it is not on the compositor's `PATH` on NixOS - journal logs `` `xrdb` not found, cannot update Xresources `` twice at Xwayland start, `xrdb -query` on `:1` is empty, so libXcursor falls back to its screen-height default (1440/48 ≈ 30 → theme size 32/48 vs the compositor's 24). Not caused by this project: nixpkgs' `cosmic-comp` package (verified on the cache-served stock 1.8.0 build: no `PATH` in the wrapper, no `xrdb` in the closure), `nixos-cosmic`'s package (wraps `xwayland` but not `xrdb`) and Pop!_OS' own `debian/control` all omit it - Pop!_OS only works because `x11-xserver-utils` arrives via the desktop meta-package. Fix in this project: `preFixup` adds `xrdb` to the wrapped `PATH` in the overlay's `nix/cosmic-comp-crane.nix` (and the NixOS host's `specialisations/cosmic-comp-crane.nix` copy), the pattern nixpkgs uses for `cosmic-initial-setup`'s `killall`. This local change stays even after the upstream PR merges: the crane derivation replaces nixpkgs' `package.nix` rather than overriding it. Verified live 2026-09-20 by running `xrdb -merge` manually on `:1` (newly started X clients pick the size up; running ones need a relaunch). | upstream (packaging) | 🟡 fix applied 2026-09-20, unverified in a booted build | [NixOS/nixpkgs#565127](https://github.com/NixOS/nixpkgs/issues/565127) (this bug, filed 2026-09-20)<br>[NixOS/nixpkgs#437281](https://github.com/NixOS/nixpkgs/issues/437281) (adjacent: Steam cursor theme)<br>[pop-os/cosmic-comp#817](https://github.com/pop-os/cosmic-comp/issues/817) → [pop-os/cosmic-epoch#1218](https://github.com/pop-os/cosmic-epoch/issues/1218) (same symptom family under scaling, pre-#1976) | [pop-os/cosmic-comp#1976](https://github.com/pop-os/cosmic-comp/pull/1976) (merged 2026-01-09; introduced the `xrdb` call-out)<br>[pop-os/cosmic-comp#2219](https://github.com/pop-os/cosmic-comp/pull/2219) (open; adds `xrdb` to upstream's nix dev shell only)<br>nixpkgs PR pending (branch `Pheoxy/nixpkgs:cosmic-comp-xrdb`) | NixOS/nixpkgs#565127 |
| **Vulkan screencopy failed on every output / screen sharing produced no frames** - `Failed to screencopy: Unsupported Vulkan renderer operation: blit source usage` (`src/backend/kms/surface/mod.rs:1647`): the exported swapchain image was not created with blit-source usage. `take_screencopy_frames`/`send_screencopy_result_vulkan` is the single path for one-shot screenshots and share sessions, so both symptoms were one bug. (Merged from two rows: same root cause.) | this project | 🟢 fixed; verified 2026-09-19 (Discord share produces frames) and 2026-09-20 (1,517 captures serviced) | n/a - this project's own Vulkan screencopy implementation | n/a | [#5](https://github.com/Pheoxy/cosmic-vulkan/issues/5) |
| **Screen share collapses the desktop frame rate** (user report 2026-09-19: stream at 5 fps → desktop feels like 5 fps; laggy in general during a share). Two mechanisms, both this project's, measured 2026-09-20 on the instrumented build `ff04b1ee` during a Discord share of DP-3 ([research/screenshare-frame-coupling.md](research/screenshare-frame-coupling.md)):<br>(1) **Whole frames refused** - `Failed to submit rendering … dmabuf render-target still locally owned` ~20×/s for the entire share (863 in 62 s; 889 over the 10:32-10:39 session, on both outputs), plus a worse minute at 10:33:11-10:34:12 with 941 portal-side `screencopy failed: Value(Unknown)` and 276 captures failing outright with the same error (`Failed to release screencopy render target`). Cause: `blit_frame_result` binds the swapchain slot for the capture blit (`frame_result.rs:358`) and never releases it, so the next scanout submit for that slot is refused - a third of all 60 Hz frames thrown away, which *is* the observed "desktop at the stream's rate". Fix: release the bind after the capture blit. Top priority.<br>(2) **Per-capture readback cost** - under Vulkan output capture is SHM-only (`constraints_for_output` falls back because `glow_renderer()` is `None` for `MultiVulkan`; upstream advertises dmabuf), every capture is a blocking blit + fresh 14.7 MB staging allocation + fence wait + memcpy, `age=0` forces a full-frame copy, and the readback runs after `queue_frame` on the output's render thread: 1,489 captures/62 s at 13 ms avg (max 34) = 30-40% of the render thread. Fix shape: advertise dmabuf constraints under Vulkan; async SHM path (fence + memcpy on vblank; deferral plumbing exists at `render.rs:259-376`); real damage age. | this project | 🔴 open, both mechanisms measured and confirmed 2026-09-20 | Not filed upstream | n/a | not yet filed |
| **Window/toplevel share dies silently** - the window-capture path (`image_copy_capture/render.rs` `render_session`/`submit_buffer`, main thread) ended a Discord window share at 10:39:58 with portal `screencopy failed: Value(Stopped)`; Discord's UI kept showing the stream as live (the user's "5 fps stream" was this). That path has no instrumentation, unlike the output path. Next build: per-capture probe on `render_session`. | ⚪ not yet determined | 🔴 open, observed once 2026-09-20 | n/a | n/a | not yet filed |
| **Screenshot capture silently fails under GPU load** - the deferred SHM-copy safety-net timeout races the real render fence. | this project | 🔴 open | n/a - not an upstream code path | n/a | [#3](https://github.com/Pheoxy/cosmic-vulkan/issues/3) |
| **`cosmic-screenshot` hung in the portal backend** (2026-09-19): the portal never returned; cleared after restarting `xdg-desktop-portal-cosmic` plus an unexplained delay, and worked without a reboot afterwards. Never root-caused; may be the row above or a portal-side wait. | ⚪ not yet determined | 🟢 cleared, cause unknown | n/a | n/a | not yet filed |
| **Cross-thread texture-cache race**: `MultiTexture::from_surface` wipes a surface's per-device texture cache in one step on buffer size/format change and re-imports separately; each output renders on its own thread, so one thread can be mid-clear while another is mid-read. Symptoms: Discord freezes during share, tiling sluggishness (every sibling resizes at once), gaming stutter varying with NVIDIA power state. Diagnostics `73d9dc8c`, `3b1e5b5c`, `ae047ce4`, `1d8cf5cc`, `3dbc4283`, `686f7f97`; generation gating `ba641438` closes the mis-insert half; a genuine cache miss in `render_texture_from_to` still warns and skips the surface for that frame. Kernel-side signature 2026-09-17: `NVRM: dmaAllocMapping_GM107: can't update VA space` bursts (33+/min) alongside the warning. **Confirmed live 2026-09-20**: `Failed to render texture, import for wrong device?` ×8 in 20 min on the render thread (`ThreadId(10)`), each with an empty per-device map (`textures: {}`) for a 1×1 `XR24` I915 Y-tiled buffer, generation 1 - i.e. Wine/Steam placeholder windows hitting the clear/read window. Fix shape: generation check + re-import fallback on the render thread. | this project | 🟡 corruption half fixed; drop-frame-on-miss open, confirmed 2026-09-20 | n/a - Vulkan multi-GPU path specific to this project | n/a | not yet filed |
| **Intermittent hardware cursor-plane assignment failure** (software cursor fallback). Downgraded 2026-09-20 ([research/cursor-pipeline-end-to-end.md](research/cursor-pipeline-end-to-end.md)): all 33 `cursor plane assignment missed` events across 10 boots are one-shot misses at DPMS wake / lock-screen unlock, each paired with a modeset (`Failed to destroy old mode property blob`); Smithay's `try_assign_cursor_plane` tests with `allow_modeset = false` while a modeset is pending, the kernel returns `-EINVAL`, the software cursor lasts one frame. Zero misses in the 4.5 h session of 2026-09-19. The earlier "tied to frame rate" framing was wrong. Monitors are driven by the Intel i915 (its cursor plane; no cross-GPU cursor import) - NVIDIA only renders Proton games. | upstream | 🟢 cosmetic | [pop-os/cosmic-comp#2664](https://github.com/pop-os/cosmic-comp/issues/2664), [#1379](https://github.com/pop-os/cosmic-comp/issues/1379), [#2113](https://github.com/pop-os/cosmic-comp/issues/2113) | Not yet researched | [#2](https://github.com/Pheoxy/cosmic-vulkan/issues/2) |
| **`GAMMA_LUT` fails with `EINVAL` on Intel multi-segment gamma hardware** (`GAMMA_LUT_SIZE`=262145 vs legacy 256 - i915's multi-segment LUT rejects a flat linear ramp at that size; NVIDIA CRTCs report 1024). Screen filter's colourblind/greyscale modes are pure `CTM` and unaffected; `invert` needs `GAMMA_LUT` and under Vulkan has no shader fallback (never built, see FEATURES.md) so on this hardware under Vulkan `invert` has no effect. Night light is upgraded to hardware `CTM` when the ramp is a per-channel scale (`diagonal_scale_from_ramp`). | this project | 🟡 fix applied, needs a boot to verify; `invert`-under-Vulkan gap remains | n/a - this project's flat-LUT encoding vs i915's format | n/a | not yet filed |
| **Duplicate per-output render threads** - `apply_config_for_outputs` excluded already-surfaced connectors by `Output` identity instead of connector handle, so a second config-apply pass could spawn a second thread for the same connector. | upstream | 🟢 fixed and verified (exactly one `Initializing drm surface` per connector) | Not filed upstream | n/a | [#1](https://github.com/Pheoxy/cosmic-vulkan/issues/1) |
| **Black-screen flash between `cosmic-greeter` and the session** (and on Plymouth → greeter). Root cause: `systemd-logind`'s `session_restore_vt()` forces an fbcon restore when the greeter's compositor drops its DRM controller, with no awareness that the successor session already owns the VT - a logind behaviour, not a Smithay `Drop` ordering problem. A proper fix is a logind change; the workaround explored here is a holder process that keeps the greeter's DRM fds and seat control open until the next greeter start (committed locally on the cosmic-comp/greeter branches, **unvalidated** - see the nixosTest plan). Deprioritised 2026-09-17. | upstream (logind) | 🔴 open, workaround unvalidated | n/a | n/a | not yet filed |
| **Handoff-holder experiment crashed the whole session** ~15 min after login (VT-state race hypothesis, unproven) - history for the row above. Reverted in cosmic-comp `c074f221` and cosmic-greeter `4217c4ac`; the open risk that motivated the nixosTest plan is that the holder never reads its inherited logind D-Bus socket, so an unread queue could eventually disconnect it and reproduce the blank mid-session. | this project | 🟢 reverted; kept as history | n/a | n/a | not yet filed |
| **Parallel Nix builds fill the zram-only swap and hard-freeze the machine** (cause of the 2026-09-18 crash; near-miss 2026-09-19 while gaming with zram 124 MiB from full). Not a compositor bug - the constraint is peak RSS (cosmic-comp's fat-LTO link ≈ 6.6 GiB), not core count. Mitigation is procedural: `nix.settings` `max-jobs = 1`, `cores = 6` (already in the host config), heavy derivations built one at a time, never the cosmic-comp release link while a game is running, and a watchdog that steps a build down to 1×1 on memory pressure. | environment | 🟢 mitigated procedurally | n/a | n/a | n/a |

Feature gaps (not bugs - things this project's Vulkan renderer doesn't do yet
at all, like screen filter/zoom postprocessing) are tracked in
[FEATURES.md](FEATURES.md) instead, not here.

## Why this table exists

A bug reproducing on hybrid Intel+NVIDIA hardware while stress-testing under
real GPU load (games, screen sharing) is easy to misattribute to whichever
renderer happens to be active at the time. Several of the issues above were
initially assumed to be Vulkan-specific and turned out, on closer
investigation (source tracing, live process/kernel introspection, and
checking upstream's own issue tracker), to be pre-existing and
renderer-agnostic. Keeping that distinction explicit and sourced is the point
of this file.
